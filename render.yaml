services:
  - type: web
    name: crudewatch
    runtime: docker
    plan: free
    healthCheckPath: /_stcore/health
    autoDeploy: true
    envVars:
      - key: CRUDEWATCH_LOW_MEMORY
        value: "true"
